package {{PackageName}}.service;

import {{PackageName}}.entity.{{ClassName}};
import {{PackageName}}.repository.{{ClassName}}Repository;
import org.springframework.stereotype.Service;
import lombok.RequiredArgsConstructor;

import java.util.*;

@RequiredArgsConstructor
@Service
public class {{ClassName}}Service{
    private final {{ClassName}}Repository repo;

    public {{ClassName}} findById({{IdType}} id){
        return repo.findById(id).orElseThrow(() -> new RuntimeException("Could not find {{ClassName}} with id " + id));
    }

    public List<{{ClassName}}> findAll(){
        return repo.findAll();
    }

    public {{ClassName}} save({{ClassName}} entity){
        return repo.save(entity);
    }

    public void delete({{IdType}} id){
        repo.deleteById(id);
    }
}

//Generated by Sprout