package {{PackageName}}.service;

import {{PackageName}}.entity.{{ClassName}};

import {{PackageName}}.dto.{{ClassName}}DTO;
import {{PackageName}}.mapper.{{ClassName}}Mapper;
import {{PackageName}}.repository.{{ClassName}}Repository;
import org.springframework.stereotype.Service;
import lombok.RequiredArgsConstructor;

import java.util.*;

@RequiredArgsConstructor
@Service
public class {{ClassName}}Service{
    private final {{ClassName}}Repository repo;
    private final {{ClassName}}Mapper mapper;

    public {{ClassName}}DTO findById({{IdType}} id){
        return mapper.toDto(repo.findById(id).orElseThrow(() -> new RuntimeException("Could not find {{ClassName}} with id " + id)));
    }

    public List< {{ClassName}}DTO > findAll(){
        return repo.findAll().stream().map(e -> mapper.toDto(e));
    }

    public {{ClassName}}DTO save({{ClassName}}DTO dto){
        return mapper.toDto(repo.save(mapper.toEntity(dto)));
    }

    public void delete({{IdType}} id){
        repo.deleteById(id);
    }
}

//Generated by Sprout